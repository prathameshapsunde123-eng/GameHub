let obj ={
    item : "smartphone",
    price : 20000
}

let str = `item is ${obj.item} and its price ${obj.price}`;
console.log(str);
console.log("item is ",obj.item, "and its price",obj.price);




let str2 = "  Hello";
console.log(str2[2]);

console.log(str2.toUpperCase());
console.log(str2.toLowerCase());
console.log(str2.trim());
console.log(str2.length)
